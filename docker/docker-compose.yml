services:
  playwright-tests:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: playwright-bdd-allure
    ipc: host
    volumes:
      - ../reports:/app/reports
    env_file:
      - ../envs/.env
    environment:
      - HEADLESS=true
    command: npm run test:login:allure
